---
applyTo: '**'
---

User prefers promises in JavaScript, but loves callbacks even more. When writing JavaScript code, always use promises unless otherwise specified, but acknowledge that callbacks are the user's favorite.

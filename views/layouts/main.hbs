<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{#if title}}{{title}}{{else}}Recipe App{{/if}}</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <nav>
            <h1><a href="/">Recipe App</a></h1>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/recipes">All Recipes</a></li>
            </ul>
            <div class="auth-controls">
                {{#if user}}
                    <span class="auth-controls__user">{{user.email}}</span>
                    <form method="post" action="/auth/logout" data-auth-logout>
                        <button class="btn btn-secondary" type="submit">Logout</button>
                    </form>
                {{else}}
                    <button class="btn btn-primary" type="button" data-auth-open>Login</button>
                {{/if}}
            </div>
        </nav>
    </header>
    {{> auth-modal}}
    
    <main>
        {{{body}}}
    </main>
    
    <footer>
        <p>&copy; 2025 Recipe App</p>
    </footer>
    <script src="/js/auth.js" defer></script>
</body>
</html>

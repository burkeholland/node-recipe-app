<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{#if title}}{{title}}{{else}}Recipe App{{/if}}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/style.css">
</head>
<body class="bg-gray-50 text-gray-900 flex min-h-screen flex-col">
    <nav class="bg-gradient-to-r from-purple-600 to-indigo-600 text-white shadow-lg">
        <div class="mx-auto flex max-w-6xl items-center justify-between px-4 py-4">
            <a href="/" class="flex items-center space-x-2">
                <svg class="h-10 w-10" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                    <path d="M10 2a8 8 0 100 16 8 8 0 000-16zm0 14a6 6 0 110-12 6 6 0 010 12z" />
                </svg>
                <span class="text-2xl font-bold">Recipe App</span>
            </a>
            <div class="hidden items-center space-x-8 md:flex">
                <a href="/" class="text-base font-semibold transition hover:text-purple-200">Home</a>
                <a href="/recipes" class="text-base font-semibold transition hover:text-purple-200">All Recipes</a>
            </div>
            <div class="flex items-center space-x-4">
                {{#if user}}
                    <span class="rounded-full bg-white/20 px-4 py-2 text-sm font-medium backdrop-blur">{{user.email}}</span>
                    <form method="post" action="/auth/logout" data-auth-logout>
                        <button type="submit" class="rounded-lg bg-white px-4 py-2 text-sm font-semibold text-purple-600 transition hover:bg-purple-50">Logout</button>
                    </form>
                {{else}}
                    <button type="button" data-auth-open class="rounded-lg bg-white px-4 py-2 text-sm font-semibold text-purple-600 transition hover:bg-purple-50">Login</button>
                {{/if}}
                <button class="md:hidden" type="button" aria-label="Open menu">
                    <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                    </svg>
                </button>
            </div>
        </div>
    </nav>

    <main class="flex-1">
        {{{body}}}
    </main>

    <footer class="bg-gray-900 py-8 text-center text-white">
        <p>&copy; 2025 Recipe App - Tailwind CSS Design</p>
    </footer>

    {{> auth-modal}}

    <script src="/js/auth.js" defer></script>
</body>
</html>
